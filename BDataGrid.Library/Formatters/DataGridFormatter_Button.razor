@inherits DataGridFormatter

<div>
    <button @onclick="() => Callback(Args.Item)">@(Args.Value?.ToString() ?? "")</button>
</div>

@code {

    [Parameter]
    public Func<object, Task> Callback { get; set; }

}
